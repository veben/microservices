database:
  url: jdbc:postgresql://localhost:5432/order
  username: postgres
  password:

spring:
  jpa:
    show-sql: true
  datasource:
    data: classpath:data/data-local.sql
    initialization-mode: always