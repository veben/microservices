database:
  url: jdbc:postgresql://developer-postgresql:5432/developer
  username: postgres
  password:

spring:
  jpa:
    show-sql: true
  datasource:
    data: classpath:data/data-local.sql
    initialization-mode: always